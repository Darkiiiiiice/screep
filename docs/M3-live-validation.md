# M3 线上验证记录

> 2026-09-15 至 2026-09-16,shard3 / W35S2,default 分支。部署链:容器维修修复版(d74a66a)→ Memory GC 版(e8ec936,bundle sha256 `ec1f2981c9a6`)。

## 回滚物料

- `artifacts/live/baseline-1789470816939/backup.json` 保存 GC 版部署前代码、Memory 与房间对象;回滚只恢复代码,不覆盖已演化的游戏 Memory。

## 验收结果(全部通过)

- **自主布局与分批施工**:extension 0 → 10/10 全部自主放置并建成(容量 300 → 800),另有 tower 工地自主放置并建成;全程无人工摆工地。
- **tower 落成**:(22,21) 5000/5000 建成,自动充能中(观测时 192/1000);logistics 已将其纳入补能 sinks。
- **受损关键结构可修复**:双源容器 hits 稳定在 ~198k/250k 区间;维修工以「容器就近取能 → 修复 → 再取能」闭环运行;保养工与矿工争座自愈(保养工转移至射程 3 的邻格远程修,继任矿工坐回容器位)。
- **Memory.creeps 幽灵清扫**:部署 130 秒内 257 条死 creep 残留 → 6 条,幽灵归零;此后每 25 tick 周期清扫,无再生。
- **控制器降级风险管理**:建设高峰期降级余量最低观测 3785(红线 3000 未触发);建设收口后能量转升级,余量开始回升;全程未发生降级。
- **长稳**:9 个监控窗口合计约 500+ 采样,心跳连续、运行错误 0、未触发自动回滚;期间人口按目标更替(1-WORK 老将正常死亡、新孵补位)。

## 2026-09-17 追加:升级服务饥饿根因与修复

建设收口后长期观测暴露一处真实设计缺陷,模拟矩阵从未覆盖:

- **现象**:降级倒计时在红线 3000 附近振荡(6070→3785→2996→3047→2996),控制器进度整窗口 +2;`Memory.controllerService` 的 `worker` 长期为空而 `lastProgress` 永远新鲜。
- **根因(互锁)**:倒计时 <3000 时 bootstrap 紧急模式让全体工人带零头能量救场(+2 进度/百 tick); crumbs 一越过红线,logistics 恢复——但 crumbs 刚刷新的 `lastProgress` 使「200 tick 停滞才任命升级员」的触发器永远不满足,专职升级员永不任命;能量流向缓冲/工地,倒计时再滑回 3000。两套应急机制互相掩盖,房间卡在「不死不活」平衡:不降级,但也永远攒不出 RCL4 进度。
- **修复**(commit 见 git log):`src/game/logistics.ts` 租约任命条件改为 `停滞≥200 tick || ticksToDowngrade<6000`(恢复带)——控制器健康由计时器水位而非进度归属定义。首版「按租约归属刷新」方案被 `test:logistics-construction` 抓出过度任命挤占经济,已改为计时器水位门控。
- **验证**:新增 2 项回归单测(恢复带内 crumbs 不阻挡任命;健康房间不白任命),红-绿双向证明;完整门禁 21/21 全绿(typecheck/lint/72 单测/build/smoke/14 场景+progression×2)。部署 sha256 `017bb2dd3c97`,回滚基线 `artifacts/live/baseline-1789616787904`。

## 未覆盖(观察项,不阻塞核心验收)

- **RCL4 到达**:RCL3→4 需 135000 进度;升级服务饥饿修复(见上节)部署后升级恢复积累,到达仍需时间验证。
- **基准入侵实测**:观测期间无敌袭;塔集火/治疗/盟友豁免由 `test:defense` 引擎场景验收。
- **storage 调度**:RCL4 解锁,模拟端已验收放置与施工;线上待 RCL4 到达。

## 监控

- `node scripts/live-monitor.mjs artifacts/live/baseline-1789616787904` 持续滚动重启(每窗口 1600 tick);心跳断档 >10 tick、人口清零或新增运行错误连续 3 次采样才自动回滚。
