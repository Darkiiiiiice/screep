{
  "name": "screep",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Screeps: World MMO AI (online-only)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src scripts",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "node scripts/deploy.mjs",
    "watch": "node scripts/watch.mjs",
    "stats": "node scripts/stats.mjs",
    "whoami": "node scripts/whoami.mjs",
    "smoke": "node scripts/smoke.mjs",
    "snapshot": "node scripts/snapshot.mjs",
    "engine": "bash scripts/engine-setup.sh",
    "scenario": "node scripts/scenario.mjs",
    "test:lifecycle": "node scripts/scenario.mjs fresh --lifecycle",
    "test:recovery": "node scripts/scenario.mjs no-memory --lifecycle --recovery",
    "test:logistics": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --logistics-recovery",
    "test:logistics-construction": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --construction",
    "test:traffic": "node scripts/scenario.mjs fresh --traffic-probe",
    "test:traffic-recovery": "node scripts/scenario.mjs fresh --traffic-recovery",
    "test:fairness": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --fairness-probe",
    "test:multi-room": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --fairness-probe --multi-room",
    "test:economy": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --economy-probe",
    "test:fairness-pressure": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --fairness-probe --population-pressure",
    "test:cpu-stress": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --fairness-probe --cpu-stress",
    "test:logistics-failure": "npm run build && node scripts/scenario.mjs fresh --lifecycle --logistics --logistics-recovery --persistent-failure",
    "select-room": "node scripts/select-room.mjs",
    "verify": "npm run typecheck && npm run lint && npm test && npm run build && npm run smoke && npm run scenario -- fresh && npm run scenario -- legacy && npm run scenario -- no-memory"
  },
  "dependencies": {
    "screeps-api": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/screeps": "^3.4.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
